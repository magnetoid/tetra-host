"""tetra-cli — command-line client for the Tetra Host control plane.

Mirrors the web dashboard against the same /api/v1 contract: sites, deploys,
live build logs, DNS, and environment variables — operable from the terminal.
"""

__version__ = "0.1.0"
